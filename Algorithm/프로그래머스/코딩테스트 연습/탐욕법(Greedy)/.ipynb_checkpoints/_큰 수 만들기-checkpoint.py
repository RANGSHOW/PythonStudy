numList = ['1924', '1231234', '4177252841']
kList = [2, 3, 4]
returnList = ['94', '3234', '775841']

number = "29495718"
k = 4
# return = "9978"

# 빼는 것이 아니라 정해진 자릿수만큼 순서에 맞게 뽑는 걸로 생각


answer = ''
answerLength = len(number) - 4

for _ in answerLength:
    if max(number)
    
    # if max(number)의 뒤에 answerLength을 충분히 뽑을 수 있다면
        # answer += max(number)
        # indexOfMaxNum = number.find(max(number))
        # number = number[0:indexOfMaxNum] + number[indexOfMaxNum + 1:]
        # 