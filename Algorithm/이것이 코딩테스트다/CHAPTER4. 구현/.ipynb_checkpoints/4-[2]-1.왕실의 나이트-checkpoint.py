# 개요
    # 나이트가 이동할 수 있는 경우의 수 출력
    # 8 X 8 좌표
    # 가로(a~h), 세로(1~8)의 좌표로 표현
# 입력 예시
    # a1: 현재 나이트의 위치
# 출력 예시
    # 2: 나이트가 이동할 수 있는 2가지 경우 (c2, b3)
    

    
if __name__ == "__main__":
    
    dx, dy = x + xMove, y + yMove
    x, y = dx, dy
    
    